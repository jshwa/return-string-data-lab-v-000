<%= form_for @product do |p| %>
  <%= p.label :name %>
  <%= p.input :name %>
  <%= p.label :description %>
  <%= p.input :description %>
  <%= p.label :price %>
  <%= p.input :price %>
  <%= p.label :inventory %>
  <%= p.input :inventory %>
  <%= p.submit %>
  <% end %>
